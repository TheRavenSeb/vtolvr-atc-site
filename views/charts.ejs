<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="author" content="Caleb Thomas (TheRavenSeb)">
    <meta name="description" content="Flight Charts - View and select various airport charts.">
    <meta name="keywords" content="Flight Charts, Airport Charts, Aviation, Flight Planning, Navigation Charts,Vtol vr">
    <title>Map Charts</title>
    <script>
        document.addEventListener('DOMContentLoaded', async() => {
            if (window.OpenKneeboard) {
                document.body.classList.add("OpenKneeboard");
                await OpenKneeboard.EnableExperimentalFeature(
                    'GraphicsTabletInfo',
                    2025012901);
                var info = await OpenKneeboard.GetGraphicsTabletInfo();
                if (info && info.SuggestedPixelSize) {
                await OpenKneeboard.SetPreferredPixelSize(
                info.SuggestedPixelSize.Width,
                info.SuggestedPixelSize.Height)}
                else{
                    // set to 600x1300 if no suggested size is provided
                    await OpenKneeboard.SetPreferredPixelSize(600, 1300);
                }
            }
            


            
        });
    </script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/p5.js/1.4.0/p5.min.js"></script>
    <script src="https://cdn.jsdelivr.net/gh/IDMNYU/p5.js-speech@0.0.3/lib/p5.speech.js"></script>
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #1e1e1e;
            color: #e0e0e0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }

        header {
            text-align: center;
            color: #e0e0e0;
            margin-bottom: 40px;
            padding-top: 20px;
        }

        header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        header p {
            font-size: 1.1em;
            opacity: 0.9;
        }

        .container {
            display: flex;
            gap: 30px;
            max-width: 1200px;
            width: 100%;
            max-height: 100%;
            flex-wrap: nowrap;
            justify-content: center;
        }

        .combined-panel {
            background: #333;
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            display: flex;
            flex-direction: column;
            gap: 20px;
            flex: 1;
            min-width: 600px;
            max-width: 1200px;
            min-height: auto;
            max-height: 100%;
            width: 100%;
        }

        .selector-panel {
            display: flex;
            flex-direction: column;
            gap: 12px;
            width: 100%;
        }

        .selector-panel h2 {
            color: #e0e0e0;
            font-size: 1.3em;
            margin: 0;
        }

        .chart-list {
            width: 100%;
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .chart-buttons-container {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            width: 100%;
        }

        .chart-btn {
            padding: 12px 16px;
            border: 2px solid #667eea;
            background: #333;
            color: #e0e0e0;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1em;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .chart-btn:hover {
            border-color: #667eea;
            background: #444;
        }

        .chart-btn.active {
            background: #667eea;
            border-color: #667eea;
            color: #fff;
        }

        .chart-btn:focus {
            outline: none;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
        }

        .display-panel {
            display: flex;
            flex-direction: column;
            gap: 20px;
            flex: 1;
            max-height: max-content;
        }

        .display-panel h2 {
            color: #e0e0e0  ;
            font-size: 1.3em;
            margin: 0;
        }

        .chart-display {
            width: 100%;
            height: 800px;
            background: #5a5a5a;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: auto;
        }

        .chart-display img {
            max-width: 100%;
            max-height: 100%;
            object-fit: contain;
            border-radius: 6px;
        }

        .placeholder {
            color: #999;
            font-size: 1.1em;
            text-align: center;
        }

        footer {
            color: white;
            margin-top: 40px;
            text-align: center;
            opacity: 0.8;
        }

        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                align-items: center;
            }

            .display-panel {
                min-width: 100%;
                max-width: 100%;
                max-height: 100%;
            }

            header h1 {
                font-size: 1.8em;
            }
        }
        @media (max-width: 480px) {
            .chart-btn {
                font-size: 0.9em;
                padding: 10px 14px;
            }
        }

        @media (max-hight: 600px) {
            .chart-display {
                height: 300px;
            }


        }

        .open-window-btn {
            background-color: #4CAF50; /* Green */
            border: none;
            color: white;
            padding: 10px 24px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 4px 2px;
            cursor: pointer;
            border-radius: 12px;
        }

        .DiscordButton {
            background-color: #7289DA; /* Discord Blue */
            border: none;
            color: white;
            padding: 10px 24px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 4px 2px;
            cursor: pointer;
            border-radius: 12px;
        }
    </style>
</head>
<body>
    <header>
        <h1>Raven's Airport Charts</h1>
        <p>Select a chart from the list to view it below. You can also use voice commands by saying "Select chart [chart name]".</p>
        <button  class="open-window-btn" onclick="openSizedWindow()">Open Charts in kneepad configuration</button> <button class="DiscordButton" onclick= "window.location.href='https://discord.gg/FYwbKdxhjY'">Join our Discord</button>
    </header>

    <div class="container">
        <div class="selector-panel">
            <h2>Available Charts</h2>
            <div class="chart-list">
                <div class="chart-buttons-container">
                    <!-- Chart buttons will be dynamically added here -->
                </div>
                <select id="chartList" style="display:none; margin-top: 20px; padding: 10px; border-radius: 8px; background: #333; color: #e0e0e0; border: 2px solid #667eea;">
                    <option value="">Select a chart</option>
                    <!-- Chart options will be dynamically added here -->
                </select>
            </div>
        </div>

        <div class="display-panel">
            <h2>Chart Display</h2>
            <div class="chart-display" id="chartDisplay">
                <div class="placeholder">Select a chart to display</div>
            </div>
        </div>
    </div>

    <footer>
        <p>&copy; 2026 Flight Charts. All rights reserved.</p>
    </footer>

    <script>
        const chartList = document.getElementById('chartList');
        const chartDisplay = document.getElementById('chartDisplay');
        let charts = [];
        
        function openSizedWindow() {
        // Opens a new window with specified dimensions
       
        window.open('/charts', 'ChartsWindow', 'width=600,height=1300');
        }

        // Fetch available chart images from assets folder
        async function loadCharts() {
            try {
                // Common chart image extensions
                const imageExtensions = ['.png'];
                const assetPath = 'https://atc-vtolvr.site/assets/'; // Update with your actual asset path

                
                const chartNames = [
                    "Shark",
                    "Military",
                    "Luna",
                    "Island Air base",
                    "Manchester Airport(KMHT)",
                    "SKYHAVEN(KDAW)"
                ];

                // Try to load each chart image
                for (const name of chartNames) {
                    for (const ext of imageExtensions) {
                        const img = new Image();
                        const src = assetPath + name + ext;

                        img.onload = function() {
                            if (!charts.find(c => c.name === name)) {
                                charts.push({
                                    name: name.replace(/-/g, ' ').toUpperCase(),
                                    src: src
                                });
                                renderChartList();
                            }
                        };

                        img.src = src;
                    }
                }

                // Fallback: If no images found, show placeholder
                setTimeout(() => {
                    if (charts.length === 0) {
                        chartList.parentElement.innerHTML = '<p style="color: #999;">No charts found in assets folder</p>';
                    }
                }, 2000);

            } catch (error) {
                console.error('Error loading charts:', error);
                chartList.innerHTML = '<p style="color: #999;">Error loading charts</p>';
            }
        }

        function renderChartList() {
            
            const container = document.querySelector('.chart-buttons-container');
            if (container) {
                while (container.children.length > 0) {
                    container.removeChild(container.lastChild);
                }
            }

            // Add chart buttons
            charts.forEach((chart, index) => {
                const button = document.createElement('button');
                button.className = 'chart-btn';
                button.textContent = chart.name;
                button.dataset.index = index;
                button.addEventListener('click', () => selectChart(chart));
                container.appendChild(button);
            });
            // Add chart options to dropdown
            charts.forEach((chart, index) => {
                const option = document.createElement('option');
                option.value = index;
                option.textContent = chart.name;
                chartList.appendChild(option);
            });
        }

        function selectChart(chart) {
            // Display the chart image

            chartDisplay.innerHTML = `<img src="${chart.src}" alt="${chart.name}" height="750px">`;
        }
        
        // Handle dropdown change
        chartList.addEventListener('change', function() {
            if (this.value !== '') {
                const chart = charts[this.value];
                selectChart(chart);
            }
        });


        // allow for speech to change charts
        var speech = new p5.SpeechRec();
        var speak = new p5.Speech(); // speech synthesis object
        speech.onResult = showResult;
        speech.start(true, false); // continuous, interimResults
        
        

        function showResult() {
            if (speech.resultValue) {
                const spokenText = speech.resultString.toLowerCase();
                console.log('Heard: ' + spokenText);
                if (spokenText.includes('select chart')) {
                    var chartName = spokenText.replace('select chart', '').trim();
                    
                
                charts.forEach((chart, index) => {
                    if (chartName.includes(chart.name.toLowerCase())) {
                        speak.speak('Loading ' + chart.name + ' chart');
                        chartList.value = index;
                        selectChart(chart);
                    }
                });
            }
            }
        }


       
        // Initialize
        loadCharts();

        
    </script>
    
</body>
</html>